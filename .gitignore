# secrets — never commit anything resembling a token or key
.env
.env.*
*.pem
*.key

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# local notes — this repo is a public web root and Pages serves every
# tracked file, including dotfiles, so nothing local belongs in git
NOTES.md
scratch/
CLAUDE.md
.claude/

# raw asset drop-off used to build writeups; not for publishing
ddd/
